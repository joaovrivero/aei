// Theme configuration is now handled by Tailwind CSS and theme-context
// This file is kept for compatibility but will be removed in the next phase

const theme = {
    config: {
        initialColorMode: 'dark',
        useSystemColorMode: true
    }
}

export default theme